{
  "name": "install",
  "version": "0.0.0",
  "scripts": {
    "postinstall": "rm -fr lib; coffee -cb install.coffee"
  },
  "dependencies": {
    "async": "^0.9.0",
    "coffee-script": "^1.8.0",
    "deploy-tools": "^0.1.0"
  }
}
