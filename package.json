{
  "name": "nechifor-machine",
  "version": "0.0.0",
  "author": "Paul Nechifor <paul@nechifor.net> (http://nechifor.net/)",
  "scripts": {
    "make": "npm install && gulp make",
    "up": "npm run make && vagrant up --provision local && npm run update",
    "up-remote": "vagrant plugin install vagrant-digitalocean && vagrant up remote --provider=digital_ocean && vagrant ssh remote -- -t 'sudo node /opt/synced/install/install.js'",
    "update": "vagrant ssh local -- -t 'sudo node /opt/synced/install/install.js'",
    "update-remote": "vagrant ssh remote -- -t 'sudo node /opt/synced/install/install.js'",
    "deploy-remote": "vagrant rsync remote && npm run update-remote"
  },
  "dependencies": {
    "async": "^0.9.0",
    "coffee-script": "^1.8.0",
    "git-require": "^0.2.0",
    "gulp": "^3.8.10"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/paul-nechifor/nechifor-machine"
  },
  "license": "MIT"
}
